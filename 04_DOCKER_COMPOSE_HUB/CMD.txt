# 이미지 생성
docker build -t [이미지명:태그명] [도커파일위치]

# 이미지 확인
docker images

# 이미지 삭제
docker rmi [id or 이미지명]

# 도커 이미지 내 진입
docker run -it  [id or 이미지:태그명] sh

#임시 컨테이너 생성
docker run -it --rm -p 3000:80 fn:1.1



docker run -d --name bn-container --network my-network -p 8090:8090 bn:1.1

docker tag bn	 marigold0307/test:bn
docker tag fn	 marigold0307/test:fn
docker tag redis	 marigold0307/test:redis
docker tag db	 marigold0307/test:db

docker push	 marigold0307/test:bn
docker push	 marigold0307/test:fn
docker push	 marigold0307/test:redis
docker push	 marigold0307/test:db